const useCache = () => {
  // const [cache, setCache] = useState({});
  // const getCache = (key) => {
  //   return cache[key];
  // };
  // return {setCache, getCache};
};

export default useCache;
